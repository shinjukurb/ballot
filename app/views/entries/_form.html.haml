= simple_form_for(@entry) do |f|
  = f.error_notification

  .form-inputs
    = f.input :title
    = f.input :description
    = f.association :topic
    = f.association :user

  .form-actions
    = f.button :submit
